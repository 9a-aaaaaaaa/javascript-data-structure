{
  "name": "js",
  "version": "0.0.0",
  "private": true,
  "dependencies": {},
  "scripts": {
    "git": "git pull && echo '准备提交了'; sleep 1 && git add . && git commit -m",
    "postgit": "git push --all",
    "test": "echo \"Error: no test specified\" && exit 1"
  }
}